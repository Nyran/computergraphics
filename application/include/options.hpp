#pragma once
#ifndef OPTIONS_HPP
#define OPTIONS_HPP

// Options struct for information about render options
struct Options {
  bool grayscale = 0;
  bool mirrorx = 0;
  bool mirrory = 0;
  bool gaussblur = 0;
};

#endif // OPTIONS_HPP